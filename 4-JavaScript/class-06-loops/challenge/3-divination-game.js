const randomNumber = 10;
let countTry;

while (true) {
    const guessNumberUser = Number(prompt("Adivinhe um número de 1 a 10"));

    if (guessNumberUser === randomNumber) {
        break;
    }
}
